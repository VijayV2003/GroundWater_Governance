# Copy this to .env.local and adjust as needed.
# The React app falls back to mock data when the backend is unreachable,
# so this is optional for local UI development.

REACT_APP_API_BASE_URL=http://localhost:8000
